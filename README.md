# PhotoBrowser

## API key
To build the project you have to supply [Flickr API key](https://www.flickr.com/services/apps/create/).

Create `./api_keys.properties` file with following content:
```
flickr_api_key=YOUR_API_KEY
```

Then run <kbd>Build</kbd> > <kbd>Make Project</kbd>.
